// Use the concatmethod in the nonMutatingConcatfunction to concatenate attachto the end of original. 
// The function should return the concatenated array.

function nonMutatingConcat(original, attach) {

[123].concat([4, 5]);

return [1, 2, 3, 4, 5];
}

var first = [1, 2, 3];
var second = [4, 5];
nonMutatingConcat(first, second);
