// Change the nonMutatingPushfunction so it uses concatto add newItemto the end of originalinstead of push. 
// The function should return an array.

function nonMutatingPush(original, newItem) {

return orginal.concat(newItem);

var first = [1, 2, 3];
var second = [4, 5];
nonMutatingPush(first, second);
